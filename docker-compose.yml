version: "3.9"

services:

  gpt-connectors:
    container_name: camunda-8-gpt-connectors
    build:
      dockerfile: Dockerfile
    networks:
      - connector-network
    env_file: ./connector-secrets.txt

networks:
  connector-network:
